/.bundle/
/.yardoc
/_yardoc/
/coverage/
/doc/
/pkg/
/spec/reports/
/tmp/
tmp/

# A library resolves its dependencies fresh on every supported Ruby, so the
# lockfile is not committed.
/Gemfile.lock

/test-results/

# rspec failure tracking
.rspec_status
